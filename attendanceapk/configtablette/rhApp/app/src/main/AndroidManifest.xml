<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    
    <application
        android:name=".ONIPApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ONIPBiometric"
        android:usesCleartextTraffic="true">
        
        <!-- Splash Activity (Entry Point) -->
        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.ONIPBiometric">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Auth Activity (Authentification par empreinte) -->
        <activity
            android:name=".activities.AuthActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ONIPBiometric" />
        
        <!-- Three Step Employee Activity -->
        <activity
            android:name=".activities.ThreeStepEmployeeActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true" />
        
        <!-- Main Menu Activity -->
        <activity
            android:name=".activities.MainMenuActivity"
            android:exported="false"
            android:theme="@style/Theme.ONIPBiometric" />
        
        <!-- Employee Enrollment Activity -->
        <activity
            android:name=".activities.EmployeeEnrollmentActivity"
            android:exported="false"
            android:theme="@style/Theme.ONIPBiometric" />
            
        <!-- Employee List Activity -->
        <activity
            android:name=".activities.EmployeeListActivity"
            android:exported="false"
            android:theme="@style/Theme.ONIPBiometric" />
            
        <!-- Settings Activity -->
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.ONIPBiometric" />
            
        <!-- Attendance Activity -->
        <activity
            android:name=".activities.AttendanceActivity"
            android:exported="false"
            android:theme="@style/Theme.ONIPBiometric"
            android:screenOrientation="portrait" />
        
    </application>
    
</manifest>

