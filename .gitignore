# ONIP — mono-repo RH + Register + APK tablettes

# Secrets et config locale
.env
.env.local
.env.*.local
!.env.example
!.env.prod.example

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/

# Django / Node
*.log
db.sqlite3
node_modules/
.next/
staticfiles/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Docker volumes locaux
*.tar

# APK builds (sources dans attendanceapk/)
*.apk
!attendanceapk/configtablette/**/*.apk

# SDK Morpho — samples (chemins trop longs Windows, non requis pour build attendance/enroll)
attendanceapk/configtablette/sdk/**/Code_Samples/
attendanceapk/.git.bak/
datas/.git.bak/

# Données sensibles (dump BD autorisé — repo privé)
datas/.env
datas/.git/
agents/
datas/Onip_Data_Carto/onip_full.sql
deploy/backups/*.sql.gz
deploy/backups/*.tar
deploy/backups/*.tar.gz
!deploy/backups/onip_rh_production.sql
