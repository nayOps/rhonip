# POS_SDK.dll (x86)

Copier depuis `C:\Users\HYF\Documents\sdk\POS-SDK\SDK\POS_SDK.dll` :

```cmd
cd device-bridge\scripts
copy-pos-sdk.cmd
```

Puis `build-pos-sidecar.cmd`.

Connexion USB par défaut : `SP-USB1` (voir Gestionnaire de périphériques / outil test TestSDKCSharp).
